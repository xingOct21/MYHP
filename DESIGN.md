# 网页设计思路

## 整体风格

极简程序员个人主页。背景使用一张图片加深色半透明渐变叠加（`rgba(15,23,36,0.18)`），既保留背景图的视觉感，又不喧宾夺主。全站统一使用等宽字体（monospace），强化"程序员"气质。文字以白色为主，配合 `text-shadow` 增强在深色背景上的可读性。

## 配色原则

几乎只用白色和透明度变体，没有彩色点缀：
- 主文字 `--text`：`#ffffff`
- 高亮 `--accent`：`#ffffff`
- 次要文字 `--muted`：`rgba(255,255,255,0.84)`
- 边框：`rgba(255,255,255,0.35)` 半透明白线，保持轻盈感

## 页面结构

从上到下信息密度递进，先展示"我是谁"，再展示"我会什么"、"我做了什么"：

1. **Header** — 大标题欢迎语
2. **个人简介** — 头像 + 姓名 / 生日 / 学历
3. **关于我** — 教育背景（本科 + 硕士）+ 爱好 + MBTI
4. **技能列表** — 编程语言、框架、工具、语言能力
5. **项目列表** — 代表性项目链接
6. **Footer** — 版权信息

## 交互设计

- 右上角固定三个国旗按钮，支持中 / 英 / 日三语切换
- 国际化逻辑用 `data-i18n` key 驱动；中文内容直接从 HTML 读取作为默认值，不单独维护中文翻译对象
- 年龄字段在页面加载时根据生日自动计算，无需手动更新
- 滚动时各 section 依次触发 `fade-in` / `visible` 动画

## 视觉层次

- 各 section 用 `border: 1px solid rgba(255,255,255,0.35)` + `border-radius: 8px` 划定区域，不遮挡背景图
- 技能用 `inline-block` 标签平铺展示
- 项目用 flex 横向卡片排列，悬停有轻微上浮效果

## 设计原则

> 用最少的颜色和装饰，靠排版、透明度和等宽字体传递「简洁、技术感」的个人品牌。
